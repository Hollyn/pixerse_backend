import type { StrapiApp } from '@strapi/strapi/admin';
import './tailwind.css';

export default {
    config: {
        locales: [],
    },
    bootstrap(app: StrapiApp) {
        // Optional bootstrap hooks
    },
};



